<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Driveon">

        <activity android:name=".ContactsActivity" />

        <service android:name=".LocationService" />

        <activity android:name=".HomeActivity" />

        <activity android:name=".LoginActivity" />

        <activity android:name=".MenuActivity" />

        <activity android:name=".BookingHistoryActivity" />

        <activity android:name=".FindFriendsActivity" />

        <activity android:name=".ProfileActivity" />

        <activity android:name=".AdminActivity" />

        <activity android:name=".HelpSupportActivity" />

        <activity
            android:name=".BookingDetailsActivity"/>

        <activity android:name=".ImagePreviewActivity" />

        <activity
            android:name=".VehicleAddAndCheckActivity"/>

        <activity
            android:name=".PendingUsersActivity"/>

        <activity
            android:name=".UserKycReviewActivity"/>

        <activity
            android:name=".RegisteredUsersActivity"/>

        <activity
            android:name=".RegisteredUserDetailsActivity"/>

        <activity android:name=".RejectedUsersActivity"/>

        <activity android:name=".RejectedUserDetailsActivity"/>

        <activity android:name=".VehicleEntryActivity"/>

        <activity
            android:name=".PostsActivity" />

        <activity
            android:name=".ReturnVehicleActivity"/>

        <activity
            android:name=".BillReviewActivity"/>

        <activity
            android:name=".PendingBillsActivity"/>

        <activity
            android:name=".BookingManagementActivity"/>

        <activity
            android:name=".ManageVehiclesActivity" />

        <activity
            android:name=".EditVehicleActivity"/>

        <activity android:name=".UsersActivity"/>

        <activity android:name=".BookingsActivity"/>

        <activity android:name=".ActiveBookingsActivity"/>

        <activity android:name=".CompletedBookingsActivity"/>







        <activity
            android:name=".SplashActivity"
            android:exported="true">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity android:name=".MainActivity"/>

        <activity android:name=".ForgotPasswordActivity"/>

        <activity android:name=".PhoneNumberActivity"/>

    </application>

</manifest>